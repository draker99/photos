<?xml version="1.0" encoding="utf-8"?>


<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:Photos" 
    x:Class="Photos.PhotosPage"
    xmlns:cv="clr-namespace:CarouselView.FormsPlugin.Abstractions;assembly=CarouselView.FormsPlugin.Abstractions">

   

 <ContentPage.ToolbarItems>  
  <ToolbarItem  Text="Add"
                Priority="1" Order="Primary"
                Clicked="GetPhoto"
                Icon="plus-simple-7.png"

                />  
  <ToolbarItem  Name="Comment" Text="Comment"  
                Priority="1" Order="Primary"
                Clicked="AddComment"
                Icon="message-7.png"
                />  
  </ContentPage.ToolbarItems>  
<Grid>
  <Grid.RowDefinitions>
    <RowDefinition Height="5"/>
    <RowDefinition Height="*" />
    <RowDefinition Height="*" />


  </Grid.RowDefinitions>
  <Grid.ColumnDefinitions>
    <ColumnDefinition Width="*" />
  </Grid.ColumnDefinitions>

<!-- ItemsSource="{Binding UserPicture}" -->
  <ContentView Grid.Row="1" Grid.Column="0">
   <cv:CarouselViewControl 
                PositionSelected="pos_sel" 
                AnimateTransition="true" 
                ShowArrows="true" 
                Orientation="Horizontal" 
                x:Name="CarouselPics" 
                ArrowsBackgroundColor="Red"
                >
    <cv:CarouselViewControl.ItemTemplate>
      <DataTemplate>
          <Image Source="{Binding Picture}"/>          
      </DataTemplate>
    </cv:CarouselViewControl.ItemTemplate>
  </cv:CarouselViewControl>
  </ContentView>      

  <Label x:Name="l_counter" Text="Please select your pictures with the + sign!" Grid.Row="2"></Label>     
  
  <ScrollView Grid.Row="3" Grid.Column="0" Orientation='Both'>              
    <ListView  x:Name="lstView" ItemTapped="tapped_listview">
            <ListView.ItemTemplate >
                <DataTemplate>
                    <ViewCell>                       
                            <Label Text="Test"/>                        
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollView>
  



  <!--
    <cv:CarouselViewControl x:Name="carousel"
        ItemsSource="{Binding MyItemsSource}"
        ShowArrows="true"
        ShowIndicators="true"
        PositionSelectedCommand="{Binding MyCommand}"
        Orientation="Horizontal">
    </cv:CarouselViewControl>
   -->
  <!--
  <ScrollView Grid.Row="2" Grid.Column="0" Orientation='Both'>         
          
    <ListView  x:Name="lstView" ItemTapped="tapped_listview">
            <ListView.ItemTemplate >
                <DataTemplate>
                    <ViewCell>                       
                            <Image Source="{Binding Picture}"/>                        
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollView>
  -->
</Grid>

   

</ContentPage>
